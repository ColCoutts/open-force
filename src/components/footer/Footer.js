import React from 'react';
import { StyledFooter } from './styled-footer/StyledFooter';

function Footer() {
  return (
    <StyledFooter>
      <p>&copy; Just trying to hang in there</p>
    </StyledFooter>
  );
}

export default Footer;
